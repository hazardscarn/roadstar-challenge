# Build Log Index

One file per major step, each covering what was built, why, and any real problem hit along the
way (not a sanitized summary — includes dead ends and fixes). Read in order for the full story,
or jump to the one you need.

| # | File | Covers |
|---|---|---|
| 1 | [01_architecture_and_planning.md](01_architecture_and_planning.md) | The architecture spec (`research/roadstar_platform_plan.md`) and the implementation-sequence plan; what changed from the two earlier (superseded) drafts |
| 2 | [02_infrastructure_setup.md](02_infrastructure_setup.md) | Supabase schemas, the remote/local database split and why, OSRM self-hosting (and the arm64 Docker fix), all 10 SQL migrations, the geofence function |
| 3 | [03_run_type_classifier.md](03_run_type_classifier.md) | `sim/classify.py` — promoting the notebook's run-type classifier to reusable code, the 8-category-vs-6-category bug this guards against, `sim/config.py`'s domain constants |
| 4 | [04_location_sourcing.md](04_location_sourcing.md) | Where simulated/live order coordinates come from: the 59 real shippers (Nominatim) + the synthetic facility pool (Overpass), why OSRM isn't part of this step, and the coverage-box geometry corrections |
| 5 | [05_ground_truth_loading.md](05_ground_truth_loading.md) | Loading the real Excel export into `ground_truth.*`, the Known Issue fixes applied, and a new duplicate-primary-key issue found (not in the original 12) |
| 6 | [06_status_state_machine.md](06_status_state_machine.md) | The simulation's trip status state machine (`sim/engine/state.py`) — why it replaces `run_type` as the live model, a bug caught by its own test, and two transition priors corrected against real data |
| 7 | [07_reward_function_and_risk_factors.md](07_reward_function_and_risk_factors.md) | The reward function, the HOS engine (daily + weekly cycle), truck maintenance risk (synthesized, with a real `BREAKDOWN` state), and the `live.trip_log`/rating views |
| 8 | [08_calibration_and_routing.md](08_calibration_and_routing.md) | `calibration.*` seeded from the notebook's known-good numbers plus two freshly-computed tables (`lane_frequency`, `order_arrival_rate`), and the OSRM route cache (`calibration.lane_routes`) — scope decision (hub-anchored, not full pairwise) and why |
| 9 | [09_discrete_event_simulation.md](09_discrete_event_simulation.md) | `sim/engine/policy.py` (epsilon-greedy) and `sim/engine/run_sim.py` (the actual sim loop) — first validated run (101/101 trips assigned over 168h), two real bugs found (Decimal/float, a missing HOS reset that silently stranded the whole fleet), and why average reward comes out negative under a no-value-function greedy policy |
| 10 | [10_run_sim_fleet_and_schema_audit.md](10_run_sim_fleet_and_schema_audit.md) | Driver↔truck pool redesign (neither a fixed 1:1 pairing nor a full 131×131 cross — checked the real data first, found no per-trip truck history exists), plus a full schema audit answering specific questions (trip_number nulls, driver_equipment's size) — points to the new `../schema_reference.md` and two draw.io diagrams |
| 11 | [11_reward_and_decision_state_fixes.md](11_reward_and_decision_state_fixes.md) | Three bugs found building the training pipeline (reward_total and decision-time driver state weren't persisted; deadhead_miles was secretly a dollar figure), `sim/training/extract_transitions.py` built for real (1.07M rows), scaling to a 10,000-run batch (`sim/run_batch.py`, 99s), the decision not to scale further, and directly verifying the engine is a real time-ordered simulation with correct reward arithmetic |
| 12 | [12_feature_richness_ftl_ltl_and_lateness.md](12_feature_richness_ftl_ltl_and_lateness.md) | Why the value function had ~0 R² (breakdown risk invisible, deadhead a flat coin flip) and the fix (R²→0.12); FTL vs LTL revenue modeling (secondary-pickup revenue finally wired in); a second (calendar-time) maintenance dimension; a real, convex appointment-lateness penalty; several more requested features; and the honest limitation this surfaced — single-trip value vs. real sequential state-value |
| 13 | [13_state_value_fitted_iteration.md](13_state_value_fitted_iteration.md) | A real state-value V(position, hos, time) via Fitted Value Iteration — the piece that actually answers the "would this prefer a Kitchener order for a Milton→London truck" question; capturing real next-state (previously always null); convergent R² 0.046→0.062 over 4 rounds; wired into a reusable scoring function, with an honest caveat about validating specific geographic predictions before trusting them |
| 14 | [14_region_clustering_and_ranker_negative_result.md](14_region_clustering_and_ranker_negative_result.md) | k-means region clustering (silhouette-selected k=30) replaces raw lat/lon, verified against real data (the London hub's 33% traffic share, -240 avg value, explained by real deadhead economics); a learning-to-rank attempt built honestly and reported as a negative result rather than spun |
| 15 | [15_research_grounding_and_evaluation_bug.md](15_research_grounding_and_evaluation_bug.md) | Literature grounding for the whole approach (DiDi KDD 2018, Powell's fleet ADP) confirming the architecture matches production dispatch systems; a real evaluation bug found and fixed (42.7%→98.6%, and why even the corrected number can't validly compare policies — off-policy evaluation); the hub-deadhead finding corroborated independently in the real historical export |
| 16 | [16_reward_realism_and_temporal_realism.md](16_reward_realism_and_temporal_realism.md) | Sourced fixes to linehaul rate (flat→distance-tiered), breakdown cost (two inconsistent figures→one), and the post-completion-deadhead ramp (found empirically backwards, replaced); H3 hexagons replace k-means regions; a structural redesign — real order lead time, a 24h decision cutoff, mid-route drivers as real candidates via projected next-state, just-in-time departure (not instant), decaying epsilon, training over a past calendar year; expected-lateness pricing added and a real ETA-basis bug caught while adding it |
| 17 | [17_truck_maintenance_feature_and_regression.md](17_truck_maintenance_feature_and_regression.md) | Adding truck-condition as a genuinely missing V(s) feature (confirmed real via Round-0 R² 0.003→0.14, not leakage); the regression it caused (t=-4.68, an extreme unregularized cliff dominating every score) found via the paired significance test itself and traced to a concrete mechanism; the shrinkage+clip fix |
| 18 | [18_realistic_breakdown_rate_and_proactive_maintenance.md](18_realistic_breakdown_rate_and_proactive_maintenance.md) | The user's instinct confirmed right on both counts: breakdown rate was ~80x too high vs. a sourced real fleet benchmark (fixed to land on the user's explicit 1-3/5,000 target), and trucks had no path back to health (`serviced()` never called — real proactive maintenance added); two real performance bugs found and fixed (HOSLog O(n) scan, DMatrix-per-call overhead); the definitive final paired significance test (t=-1.51, not significant — a healthy, honest result after removing the earlier regression) |
| 19 | [19_real_data_replay_backtest.md](19_real_data_replay_backtest.md) | A real-data backtest (`sim/backtest/real_data_replay.py`): REAL historical dispatch vs. TRAINED model on the actual 1,667 real orders, apples-to-apples on revenue-minus-deadhead only (redesigned twice per direct user correction — no GREEDY arm, no unfair penalty charges); two real bugs caught before trusting results (a zero-lead-time construction bug, an event-loop early-exit bug); a real missed-opportunity recovery check — TRAINED found a feasible candidate for 100% of 130 real historical `was_dispatched=false` orders, ~$10.6K recovered on genuine freight (excluding real yard-shuttle moves), reported with an honest, non-oversold interpretation |
| 20 | [20_live_scoring_theory_proof.md](20_live_scoring_theory_proof.md) | `sim/live/score_quote.py` — proves the live quote-scoring data pipeline works end to end, reusing the SAME `sim/engine/policy.py` functions the simulator validated (not a parallel reimplementation); a real schema gap found and closed (`live.trips` had no destination/projected-state columns, so mid-route drivers couldn't be real candidates — extended per direct user instruction); verified against a seeded test fleet, both idle and mid-route candidate paths confirmed exercised; one deliberate, stated simplification (HOSState reconstructed conservatively from live's single hos_remaining_hours number) |
| 21 | [21_live_scoring_pickup_date_fix_and_distribution.md](21_live_scoring_pickup_date_fix_and_distribution.md) | A real bug found from the user's own instinct ("mid-route drivers should compete too, not just idle ones"): `QuoteRequest` collapsed booking time and real pickup date into one field, reintroducing the same zero-lead-time construction bug fixed once already in file 19 — confirmed via real before/after candidate scores (mid-route lateness penalties dropped from 53-544 CAD to 0-12 CAD with a real 3h lead time, and 4 of the top 10 ranked results became mid-route drivers, including the #2 overall pick on real deadhead economics); `score_quote()` now also returns the full reward distribution across every feasible candidate (revenue/deadhead/each penalty term/idle-vs-mid-route split), not just the winner |
| 22 | [22_session_summary_ml_pipeline_complete.md](22_session_summary_ml_pipeline_complete.md) | End-of-session orientation checkpoint before handing off to a fresh session for the UI/dashboard build — what's validated (sim, both models, two independent validations, live-scoring proof), what schema exists but isn't wired to any app code yet, what's not built at all (frontend, auth, invoicing, real live-data feed), and the immediate next step |
| 23 | [23_home_base_return_gap_found.md](23_home_base_return_gap_found.md) | A real gap traced end to end (reward.py, hos.py, the trained model's actual state features): nothing prices a driver ending up stranded far from base with too little HOS cycle time left, nor credits a chain of real trips that works a driver back toward base instead of one empty return leg — `home_zone`/`terminal_zone` is used only to place drivers at simulation start, `hos_remaining` is a blended min across all 5 real HOS limits that can't distinguish "recoverable tomorrow" from "genuinely stranded," and region-value is demand-driven (empirically NOT hub-proximity-driven — landing at a hub is one of the worst outcomes for needing another deadhead, per log 16). Scoped out to its own session — see [NEW_SESSION_TRAINING_RETARGET_PROMPT.md](NEW_SESSION_TRAINING_RETARGET_PROMPT.md) |

| 24 | [24_home_progress_retarget_built_and_trained.md](24_home_progress_retarget_built_and_trained.md) | The home-base-return retarget built for real: potential-based `home_progress_bonus` + `cycle_end_stranding_penalty` (HOS-cycle-urgency-weighted, not the daily clocks), new distance-to-own-home-hub and split HOS-cycle state features, a new migration, a fresh 500-run batch, both models retrained, 12 hand-built regression tests (including the user's own Milton→London→Kitchener→Mississauga chain example), and a real paired significance test — a statistically significant +10.8% overall reward improvement (p=0.0068), split honestly from a directly-measured finding that the new mechanism itself never once fired (0/8,990 trips) because this fleet's real calibrated demand never produces a genuinely cycle-tight driver at any tested run length — a real coverage gap, not a flaw, reported the same way log 14's negative result was |

_Add a new numbered file per major step going forward; update this table when you do._
